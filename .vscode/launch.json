{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run server.ts with npm start",
            "runtimeExecutable": "npm",
            "runtimeArgs": ["start", "server.ts"],
            "cwd": "${workspaceFolder}"
        }
    ]
}
